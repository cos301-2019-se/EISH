package com.monotoneid.eishms.services;

import com.fasterxml.jackson.databind.node.ObjectNode;
import com.monotoneid.eishms.model.DeviceConsumption;
import com.monotoneid.eishms.repository.DeviceConsumptionRepository;

import org.springframework.beans.factory.annotation.Autowired;

public class DeviceConsumptionService {
    /*
    @Autowired
    private DeviceConsumptionRepository deviceconsumptionRepository;

    public void insertDeviceConsumption(long id,float c,long t){
        DeviceConsumption dc =new DeviceConsumption();
        dc.setConsumption(c);
        dc.setConsumptionTime(t);
        dc.setDeviceConsumptionID(id);
        deviceconsumptionRepository.save(dc);

        
    }
*/
}